# Supaband — Python Dependencies
# Install: pip install -r requirements.txt
# Or use uv: uv pip install -r requirements.txt

# ── LLM & Agent Framework ─────────────────────────────
langchain>=1.3
langchain-openai>=0.2
langgraph>=0.2
chromadb>=0.5

# ── Band Platform SDK ─────────────────────────────────
thenvoi_rest

# ── WebUI Server ──────────────────────────────────────
fastapi>=0.100
uvicorn>=0.20
httpx>=0.24

# ── Terminal UI ───────────────────────────────────────
rich>=13.0

# ── Configuration ─────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0
